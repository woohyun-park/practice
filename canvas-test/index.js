const body = document.getElementsByTagName("body")[0];
body.style.color = "Blue";
body.style.fontFamily = "Time New Roman";

const { color, fontFamily } = body.style;

console.log(color, fontFamily);
